/**
*	@flow
*	@module home.dispatcher
*	@author Patricio Ferreira <3dimentionar@gmail.com>
**/

import _ from 'underscore';
import {dispatcher} from 'common/dispatcher/dispatcher';

/**
*	Class HomeDispatcher
*	@namespace home.dispatcher
*	@class home.dispatcher.Dispatcher
*	@extends common.dispatcher.Dispatcher
*
*	@requires underscore
*	@requires common.dispatcher.Dispatcher
**/
export const HomeDispatcher = _.extend(dispatcher, {

	// PlaceHolder: Override Specifics for HomeDispatcher

});
